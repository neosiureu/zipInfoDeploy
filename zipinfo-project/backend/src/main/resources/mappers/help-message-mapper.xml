<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.zipinfo.project.admin.model.mapper.HelpMessageMapper">

  <!-- 전체 문의 목록 조회 -->
  <select id="selectAllMessages" resultType="com.zipinfo.project.admin.model.dto.HelpMessage">
    SELECT 
      MESSAGE_NO,
      MESSAGE_TITLE,
      MESSAGE_CONTENT,
      MESSAGE_WRITE_DATE,
      MESSAGE_READ_FL,
      USER_DEL_FL,
      ADMIN_DEL_FL,
      SENDER_NO,
      RECEIVER_NO
      -- ❌ REPLY_CONTENT, REPLY_DATE 제거
    FROM HELP_MESSAGE
    WHERE USER_DEL_FL = 'N' AND ADMIN_DEL_FL = 'N'
    ORDER BY MESSAGE_NO DESC
  </select>

  <!-- 특정 메시지 상세 조회 -->
  <select id="selectMessageById" parameterType="long" resultType="com.zipinfo.project.admin.model.dto.HelpMessage">
    SELECT 
      MESSAGE_NO,
      MESSAGE_TITLE,
      MESSAGE_CONTENT,
      MESSAGE_WRITE_DATE,
      MESSAGE_READ_FL,
      USER_DEL_FL,
      ADMIN_DEL_FL,
      SENDER_NO,
      RECEIVER_NO
    FROM HELP_MESSAGE
    WHERE MESSAGE_NO = #{messageNo}
  </select>

</mapper>

